kind = "mobile"
previewPath = "/mobile/"
title = "CrossCures Mobile"
version = "1.0.0"
id = "artifacts/crosscures-mobile"
router = "expo-domain"

[[integratedSkills]]
name = "expo"
version = "1.0.0"

[[services]]
ensurePreviewReachable = "/status"
name = "expo"
paths = [ "/mobile/" ]
localPort = 24563

[services.development]
run = "pnpm --filter @workspace/crosscures-mobile run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/crosscures-mobile", "run", "build" ]
run = [ "pnpm", "--filter", "@workspace/crosscures-mobile", "run", "serve" ]

[services.env]
PORT = "24563"
BASE_PATH = "/mobile/"
